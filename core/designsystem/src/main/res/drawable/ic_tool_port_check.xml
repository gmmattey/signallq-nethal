<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <!--
      Ícone "colchetes" da entrada Verificação de porta em FERRAMENTAS AVANÇADAS (issue #136,
      protótipo `3c`/`3f`, linha ~732) — porta direta do path usado no protótipo. Cor resolvida em
      runtime via `Icon(tint = ...)` no chamador (SettingsScreen), não fixa aqui.
    -->
    <path
        android:pathData="M8,7l-5,5 5,5M16,7l5,5 -5,5"
        android:strokeColor="#8891A8"
        android:strokeWidth="1.7"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:fillColor="#00000000" />
</vector>
